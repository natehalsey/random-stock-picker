PYTHON_VERSION=$(cat .python-version | head -n 1 | tail -1)

pyenv install $PYTHON_VERSION
pyenv virtualenv $PYTHON_VERSION ${PWD##*/}.venv

source  ${PWD##*/}.venv/bin/activate

pip install pip-tools
pip-sync requirements.txt
